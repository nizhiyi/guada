name: Build GuaDa AI Electron (Windows x64)

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 60
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native modules for Electron
        run: |
          $electronVersion = node -p "require('./package.json').devDependencies.electron"
          npm rebuild --runtime=electron --target=$electronVersion --arch=x64

      # ✅ 关键修复：执行完整的构建流程，通过环境变量禁用发布
      - name: Build complete Electron application
        env:
          WIN_CSC_IDENTITY_AUTO_DISCOVERY: false
          ELECTRON: true
          # 通过环境变量告诉electron-builder不要发布，完美解决参数传递问题
          ELECTRON_BUILDER_PUBLISH: never
        run: npm run build:electron

      - name: Upload Windows build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: GuaDa-AI-Windows-x64
          path: release/GuaDa*.exe
          compression-level: 9
          if-no-files-found: error
